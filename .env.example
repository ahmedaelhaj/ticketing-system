# --- Postgres ---
POSTGRES_USER=ticketing
POSTGRES_PASSWORD=ABCeasyAS123
POSTGRES_DB=ticketing

# --- Backend ---
JWT_SECRET=ABCeasyAS123
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480

# Bootstrap super admin — created automatically on first startup ONLY IF the
# users table is empty. Editing these values later has no effect unless you
# also wipe the postgres volume (docker compose down -v) and start fresh.
FIRST_SUPER_ADMIN_EMAIL=sa@gmail.com
FIRST_SUPER_ADMIN_PASSWORD=sa@12345

# --- Nginx ---
HTTP_PORT=8080
